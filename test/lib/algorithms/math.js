'use strict';

const assert = require('assert');
const path = require('path');

const dsalg = require('../../..');

describe('Fibonacci numbers', () => {
    it('With generators', () => {
        assert.ok(true);
    });
});
